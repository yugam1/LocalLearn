{
  "//": "Java runs through .vscode/run-java.sh, which dispatches on the file's path: concurrency-lab goes to its run.sh (plain javac, no Maven), order-service tells you to use F5. NOT code-runner.executorMapByGlob \u2014 that setting matches globs against the file's basename only, never its directory, so a path-shaped glob silently never matches and falls through to executorMap.",
  "code-runner.executorMap": {
    "java": "\"$workspaceRoot/.vscode/run-java.sh\" \"$fullFileName\""
  },
  "code-runner.runInTerminal": true,
  "code-runner.saveFileBeforeRun": true,
  "java.compile.nullAnalysis.mode": "automatic",

  "rest-client.environmentVariables": {
    "$shared": {
      "host": "http://localhost:8080",
      "orderId": "1",
      "orderNumber": "ORD-0001",
      "today": "2026-09-10"
    },
    "local": {
      "host": "http://localhost:8080"
    },
    "docker": {
      "host": "http://localhost:8081"
    }
  },
  "rest-client.defaultHeaders": {
    "Accept": "application/json"
  }
}
