{
  "code-runner.executorMap": {
    "java": "cd $dir && javac -proc:full -cp \"$workspaceRoot/lib/lombok.jar\" -d . $fileName && java -cp \".:$workspaceRoot/lib/lombok.jar\" $fileNameWithoutExt"
  },
  "//": "concurrency-lab is a Maven module, not standalone files: its classes live in several packages and reference support/ and api/. Compiling one file with only lombok on the classpath cannot resolve those, so route it through source-file mode with the module's compiled classes on the classpath. Needs `./mvnw -q compile` once (and again after changing anything outside exercises/).",
  "code-runner.executorMapByGlob": {
    "**/concurrency-lab/**/*.java": "cd $workspaceRoot/concurrency-lab && java -classpath target/classes $fullFileName"
  },
  "code-runner.runInTerminal": true,
  "code-runner.saveFileBeforeRun": true,
  "java.compile.nullAnalysis.mode": "automatic",

  "rest-client.environmentVariables": {
    "$shared": {
      "host": "http://localhost:8080",
      "orderId": "1",
      "orderNumber": "ORD-0001",
      "today": "2026-09-10"
    },
    "local": {
      "host": "http://localhost:8080"
    },
    "docker": {
      "host": "http://localhost:8081"
    }
  },
  "rest-client.defaultHeaders": {
    "Accept": "application/json"
  }
}
